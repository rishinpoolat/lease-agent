# Spec: <feature name>

## Problem
What's missing or broken, in one or two sentences.

## Scope
What this feature covers. Reference the requirement ID(s) from
`docs/context/01-requirements.md` if applicable.

## Out of scope
What's explicitly not covered by this pass.

## Context layers touched
Which of `docs/context/01`–`07` this feature reads from or needs to update
(run the cascade in `.claude/rules/context-layers.md` and note the outcome
here, not just in your head).

## Open questions
Anything asked of the developer and their answer, so it's not re-asked.

## Decisions
Anything resolved during spec discussion that should become an ADR entry in
`docs/context/07-decisions.md` — link it once added.
